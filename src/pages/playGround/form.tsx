import React, { FormEvent } from 'react';
import { Input, Form, Button } from 'antd'
const { TextArea } = Input
interface IProps {
    form: any,
    dispatch: Function,
    script: string
}
const ScriptEditor = (props: IProps) => {
    return <div></div>
}
export default ScriptEditor